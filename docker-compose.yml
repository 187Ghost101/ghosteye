version: '3.8'

services:
  ghosteye:
    build:
      context: .
      dockerfile: Dockerfile
    image: 187ghost101/ghosteye:latest
    container_name: ghosteye
    ports:
      - "8082:8082"
    restart: unless-stopped
    environment:
      - TZ=UTC
